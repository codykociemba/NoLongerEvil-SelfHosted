# NoLongerEvil Server Configuration
# Copy this file to .env and customize as needed

# Server configuration
API_ORIGIN=https://backdoor.nolongerevil.com
PORT=8080
HOST=0.0.0.0
# WORKERS=4  # Worker processes (default: 2*CPU+1, max 8)

# TLS configuration (optional)
# CERT_DIR=./certs

# Entry key configuration
ENTRY_KEY_TTL_SECONDS=3600

# Weather configuration
WEATHER_CACHE_TTL_MS=600000

# Subscription configuration
# Set to 0 for infinite timeout
SUBSCRIPTION_TIMEOUT_MS=0
MAX_SUBSCRIPTIONS_PER_DEVICE=100

# Debug configuration
DEBUG_LOGGING=false
DEBUG_LOGS_DIR=./data/debug-logs

# Database configuration
SQLITE3_DB_PATH=./data/database.sqlite

# MQTT configuration (optional)
# MQTT_HOST=localhost
# MQTT_PORT=1883
# MQTT_TOPIC_PREFIX=nolongerevil
